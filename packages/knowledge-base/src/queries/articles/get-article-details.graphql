query getArticleDetails($id: Int!){
  articleDetails: article_by_pk(id: $id) {
    fields
    authors {
      author {
        email
        id
      }
    }
    subtitle
    taxonomy_items {
      taxonomy_id
    }
    title
    path
    knowledge_type
    id
    image
    status
    summary
  }
}
