mutation createUser(
  $cognitoId: String!
  $email: String!
  $signupRequest: jsonb
) {
  insert_user(
    objects: {
      cognito_id: $cognitoId
      email: $email
      signupRequest: $signupRequest
    }
  ) {
    returning {
      id
      cognito_id
      email
    }
  }
}
