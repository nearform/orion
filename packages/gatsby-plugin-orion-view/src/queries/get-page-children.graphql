query getPageChildren($page: Int!) {
  orion_page(where: {
    _and: {
      parent: { id:{ _eq: $page }},
      published: { _is_null: false },
      expires: { _is_null: true }
    }
  }) {
    path
    title
  }
}
