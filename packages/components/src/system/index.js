/** Blocks - these components are full width for the page
 * from an atomic design system perspective these can be regarded as organisms **/
export { default as SiteHeader } from './components/blocks/site-header'
