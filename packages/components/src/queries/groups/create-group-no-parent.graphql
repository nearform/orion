# See comment in components/admin/UserGroups
mutation unsafe__createGroupNoParent($name: String!, $type: String!) {
  insert_group(objects: { name: $name, type: $type }) {
    returning {
      id
      name
    }
  }
}
