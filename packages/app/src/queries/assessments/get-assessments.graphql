query getAssessments {
  assessment(order_by: { created_at: desc }) {
    id
    key
    name
    status
    created_at
    owner {
      user_groups {
        group {
          name
        }
      }
    }
  }
}
