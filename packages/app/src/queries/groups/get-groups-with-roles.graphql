query getGroups($limit: Int!, $offset: Int!) {
  group(limit: $limit, offset: $offset) {
    id
    name
    roles {
      role {
        name
      }
    }
  }
  field_aggregate: group_aggregate {
    aggregate {
      count
    }
  }
}
