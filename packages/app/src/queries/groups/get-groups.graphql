query getGroups($limit: Int, $offset: Int, $orderBy: group_order_by!) {
  group(limit: $limit, offset: $offset, order_by: [$orderBy]) {
    id
    name
    type
  }
  field_aggregate: group_aggregate {
    aggregate {
      count
    }
  }
}
