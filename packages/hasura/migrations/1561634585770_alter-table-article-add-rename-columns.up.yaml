- args:
    sql: ALTER TABLE "public"."article"
      ADD COLUMN "subtitle" text NOT NULL,
      ADD COLUMN "path" text NOT NULL,
      ADD COLUMN "status" text NOT NULL DEFAULT 'in-progress',
      ADD COLUMN "recommended" jsonb NOT NULL,
      ADD COLUMN "fields" jsonb NOT NULL
  type: run_sql

- args:
    sql: ALTER TABLE "public"."article"
      RENAME "description" TO "summary"
  type: run_sql

- args:
    sql: ALTER TABLE "public"."article"
      RENAME "content" TO "body"
  type: run_sql
