- args:
    sql: CREATE TABLE "public"."orion_user"("auth_id" text NOT NULL, "given_name"
      text, "email" text NOT NULL, "title" text, "avatar" text, "bio" text, "role_id"
      integer NOT NULL, "group_id" integer NOT NULL, PRIMARY KEY ("auth_id") , UNIQUE
      ("auth_id"));
  type: run_sql
- args:
    name: orion_user
    schema: public
  type: add_existing_table_or_view
