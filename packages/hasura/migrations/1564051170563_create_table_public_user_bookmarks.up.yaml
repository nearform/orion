- args:
    sql: CREATE TABLE "public"."user_bookmarks"("id" serial NOT NULL, "user_id" integer
      NOT NULL, "article_id" integer NOT NULL, PRIMARY KEY ("id") , UNIQUE ("id"));
  type: run_sql
- args:
    name: user_bookmarks
    schema: public
  type: add_existing_table_or_view
