- args:
    sql: CREATE TABLE "public"."orion_page_ancestry"("page_id" integer NOT NULL, "ancestor_id"
      integer NOT NULL, PRIMARY KEY ("page_id","ancestor_id") , FOREIGN KEY ("page_id")
      REFERENCES "public"."orion_page"("id") ON UPDATE restrict ON DELETE restrict,
      FOREIGN KEY ("ancestor_id") REFERENCES "public"."orion_page"("id") ON UPDATE
      restrict ON DELETE restrict);
  type: run_sql
- args:
    name: orion_page_ancestry
    schema: public
  type: add_existing_table_or_view
