- args:
    permission:
      allow_aggregations: false
      columns:
      - id
      - role_id
      - user_id
      filter:
        user:
          user_groups:
            group:
              parent_id:
                _eq: X-Hasura-Group-Id
      limit: null
    role: platform-admin
    table:
      name: user_role
      schema: public
  type: create_select_permission
