- args:
    permission:
      allow_aggregations: false
      columns:
      - id
      - name
      - parent_id
      - type
      filter:
        _or:
        - id:
            _eq: X-Hasura-Group-Id
        - parent_id:
            _eq: X-Hasura-Group-Id
      limit: null
    role: partner-admin
    table:
      name: group
      schema: public
  type: create_select_permission
