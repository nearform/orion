- args:
    sql: CREATE TABLE "public"."recommended_articles"("article_id" integer NOT NULL,
      "recommended_id" integer NOT NULL, PRIMARY KEY ("article_id","recommended_id")
      , FOREIGN KEY ("article_id") REFERENCES "public"."article"("id") ON UPDATE restrict
      ON DELETE cascade, FOREIGN KEY ("recommended_id") REFERENCES "public"."article"("id")
      ON UPDATE restrict ON DELETE cascade, UNIQUE ("article_id", "recommended_id"));
  type: run_sql
- args:
    name: recommended_articles
    schema: public
  type: add_existing_table_or_view
