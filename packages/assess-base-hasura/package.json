{
  "name": "assess-base-hasura",
  "private": true,
  "description": "Raw-salmon assess base database",
  "version": "0.1.0",
  "author": "NearForm",
  "scripts": {
    "console": "env-cmd .env hasura console",
    "migrate": "env-cmd .env hasura migrate",
    "artifact:create": "cd .. && tar -zcf $ORG-$npm_package_name-$npm_package_version-$BUILD.tar.gz assess-base-hasura",
    "artifact:name": "echo $ORG-$npm_package_name-$npm_package_version-$BUILD.tar.gz"
  },
  "devDependencies": {
    "env-cmd": "^8.0.2"
  }
}
