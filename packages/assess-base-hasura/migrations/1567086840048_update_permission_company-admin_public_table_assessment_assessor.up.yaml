- args:
    permission:
      allow_aggregations: false
      columns:
      - assessment_id
      - assessor_id
      filter:
        assessment:
          owner:
            user_groups:
              group_id:
                _eq: X-Hasura-Group-Id
      limit: null
    role: company-admin
    table:
      name: assessment_assessor
      schema: public
  type: create_select_permission
