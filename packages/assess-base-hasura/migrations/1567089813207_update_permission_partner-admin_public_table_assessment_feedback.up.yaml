- args:
    permission:
      allow_aggregations: false
      columns:
      - id
      - pillar_key
      - criterion_key
      - part_number
      - table_key
      - feedback_values
      - assessment_id
      filter:
        _or:
        - assessment:
            owner:
              user_groups:
                group_id:
                  _eq: X-Hasura-Group-Id
        - assessment:
            owner:
              user_groups:
                group:
                  parent_id:
                    _eq: X-Hasura-Group-Id
      limit: null
    role: partner-admin
    table:
      name: assessment_feedback
      schema: public
  type: create_select_permission
