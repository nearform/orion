- args:
    sql: CREATE TABLE "public"."assessment_file"("id" serial NOT NULL UNIQUE, "assessment_id"
      integer NOT NULL, "part_number" integer , "user_id" integer NOT NULL, "file_name"
      text NOT NULL, "s3_reference" text NOT NULL, "created_at" timestamp with time
      zone NOT NULL DEFAULT now(), PRIMARY KEY ("id") , FOREIGN KEY ("assessment_id")
      REFERENCES "public"."assessment"("id") ON UPDATE cascade ON DELETE cascade,
      FOREIGN KEY ("user_id") REFERENCES "public"."user"("id") ON UPDATE no action
      ON DELETE no action);
  type: run_sql
- args:
    name: assessment_file
    schema: public
  type: add_existing_table_or_view
