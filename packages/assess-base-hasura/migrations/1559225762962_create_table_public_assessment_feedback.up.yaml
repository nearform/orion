- args:
    sql: CREATE TABLE "public"."assessment_feedback"("id" serial NOT NULL, "pillar_key"
      text NOT NULL, "criterion_key" text NOT NULL, "part_number" integer NOT NULL,
      "table_key" text NOT NULL, "feedback_values" jsonb NOT NULL, "assessment_id"
      integer NOT NULL, PRIMARY KEY ("id") , FOREIGN KEY ("assessment_id") REFERENCES
      "public"."assessment"("id") ON UPDATE cascade ON DELETE cascade, UNIQUE ("id"));
  type: run_sql
- args:
    name: assessment_feedback
    schema: public
  type: add_existing_table_or_view
