- args:
    sql: CREATE TABLE "public"."assessment"("id" serial NOT NULL, "owner_id" integer
      NOT NULL, "key" text NOT NULL UNIQUE, "created_at" timestamp with time zone
      NOT NULL DEFAULT now(), PRIMARY KEY ("id") , FOREIGN KEY ("owner_id") REFERENCES
      "public"."user"("id") ON UPDATE restrict ON DELETE restrict);
  type: run_sql
- args:
    name: assessment
    schema: public
  type: add_existing_table_or_view
