- args:
    sql: CREATE TABLE "public"."assessment_assessor"("assessment_id" integer NOT
      NULL, "assessor_id" integer NOT NULL, PRIMARY KEY ("assessment_id","assessor_id")
      , FOREIGN KEY ("assessment_id") REFERENCES "public"."assessment"("id") ON UPDATE
      restrict ON DELETE restrict, FOREIGN KEY ("assessor_id") REFERENCES "public"."user"("id")
      ON UPDATE restrict ON DELETE restrict);
  type: run_sql
- args:
    name: assessment_assessor
    schema: public
  type: add_existing_table_or_view
