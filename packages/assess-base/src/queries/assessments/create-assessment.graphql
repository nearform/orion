mutation createAssessment(
  $key: String!
  $name: String!
  $owner_id: Int!
  $internal: Boolean!
) {
  insert_assessment(
    objects: {
      key: $key
      name: $name
      owner_id: $owner_id
      internal: $internal
    }
  ) {
    returning {
      id
    }
  }
}
