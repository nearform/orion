mutation updateAssessmentTableRow($id: Int!, $tableValues: jsonb!) {
  update_assessment_table(
    where: { id: { _eq: $id } }
    _set: { table_values: $tableValues }
  ) {
    returning {
      id
      table_values
    }
  }
}
