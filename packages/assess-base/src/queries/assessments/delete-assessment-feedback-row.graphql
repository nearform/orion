mutation deleteAssessmentFeedbackTableRow($id: Int!, $rowIndex: Int!) {
  update_assessment_feedback(
    where: { id: { _eq: $id } }
    _delete_elem: { feedback_values: $rowIndex }
  ) {
    returning {
      id
      feedback_values
    }
  }
}
