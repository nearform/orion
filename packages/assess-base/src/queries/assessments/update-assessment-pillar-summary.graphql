mutation updateAssessmentExecSummary($id: Int!, $pillar_summary: jsonb!) {
  update_assessment(
    where: { id: { _eq: $id } }
    _set: { pillar_summary: $pillar_summary }
  ) {
    returning {
      id
      pillar_summary
    }
  }
}
