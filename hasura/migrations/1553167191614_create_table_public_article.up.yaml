- args:
    sql: CREATE TABLE "public"."article"("id" serial NOT NULL, "author_id" integer
      NOT NULL, "title" text NOT NULL, "description" text NOT NULL, "content" text
      NOT NULL, "created_at" date NOT NULL DEFAULT NOW(), "updated_at" date NOT NULL,
      "published_at" date NOT NULL, "visible_to" integer NOT NULL, "image" text NOT
      NULL, PRIMARY KEY ("id") );
  type: run_sql
- args:
    name: article
    schema: public
  type: add_existing_table_or_view
